// import { CartItemsProps } from "../types/models";

//add to cart function
// export const addToCart = (items: CartItemsProps[]) => {
//   const itemIndex = items.findIndex((item: CartItemsProps) => item.id === id);
//   if (itemIndex >= 0) {
//     let cartItem = items[itemIndex];
//     cartItem.quantity += 1;
//     return {
//       ...items,
//       [itemIndex]: cartItem,
//     };
//   }

//   return {
//     ...items,
//     items: [...items, { id, quantity: 1 }],
//   };
// };
